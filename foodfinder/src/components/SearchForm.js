import { useGlobalContext } from "../context"

const SearchForm = () => {
    const {setSearchTerm} = useGlobalContext()
    return (
        <section>
            sEARCH
        </section>
    )
}

export default SearchForm